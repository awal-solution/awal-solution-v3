import { HeroSection } from '@src/components/heroSection/Herosection';

export const NotFound = () => {
  return (
    <div className='h-screen bg-[#E3F0FF]'>
      <HeroSection subHeading={'contact with support 😃'} heading={'Page not Found !'} />
    </div>
  );
};
